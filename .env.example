VITE_MODEL_TAG=gemma3:1b

# Optional: Gemini (cloud) provider. Leave blank to keep MindEase fully on-device.
# Read server-side by the Vite proxy at /api/gemini — never exposed to the browser bundle.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
